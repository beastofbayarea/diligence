Outputs from a demo run:

- claims.json — array of extracted claims with fields: claim, source_file, page, type
- claims_checked.json — verification results with status: verified/contradicted/unverifiable
- memo.md — final memo produced by memo()
- demo_console.log — console transcript of a run (commands and key outputs)

Include real files for the committed demo run so reviewers can reproduce the ten-minute script.